apiVersion: apps/v1
kind: Deployment
metadata:
  name: anthos-mcp
spec:
  replicas: 1
  selector:
    matchLabels: { app: anthos-mcp }
  template:
    metadata:
      labels: { app: anthos-mcp }
    spec:
      containers:
      - name: anthos-mcp
        image: us-central1-docker.pkg.dev/heckerlabs/heckathon/anthos-mcp:latest
        envFrom:
          - secretRef: { name: heckathon-secrets }
        ports:
          - containerPort: 8000
            name: http
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: anthos-mcp
spec:
  type: ClusterIP
  selector: { app: anthos-mcp }
  ports:
    - name: http
      port: 80
      targetPort: 8000

---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: mcp-healthcheck
spec:
  default:
    config:
      type: HTTP
      httpHealthCheck:
        port: 8000
        requestPath: /health
    checkIntervalSec: 300
    timeoutSec: 30
    healthyThreshold: 1
    unhealthyThreshold: 1
  targetRef:
    group: ""
    kind: Service
    name: anthos-mcp
